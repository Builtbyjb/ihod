{
  "type": "module",
  "scripts": {
    "dev-web": "npx turbo dev --filter=@invoice/web",
    "dev-server": "npx turbo dev --filter=@invoice/server",
    "dev": "npx turbo dev",
    "deploy-web": "npx turbo deploy --filter=@invoice/web",
    "deploy-server": "npx turbo deploy --filter=@invoice/server",
    "build-web": "npx turbo build --filter=@invoice/web",
    "build-server": "npx turbo build --filter=@invoice/server",
    "build": "npx turbo build",
    "lint-web": "npx turbo lint --filter=@invoice/web",
    "lint-server": "npx turbo lint --filter=@invoice/server",
    "lint": "npx turbo lint",
    "prepare": "husky"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.10",
    "husky": "^9.1.7",
    "turbo": "^2.9.8",
    "wrangler": "^4.83.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "packageManager": "npm@8.5.0",
  "dependencies": {
    "@react-pdf/renderer": "^4.5.1",
    "@shared/lib": "^0.0.1",
    "@shared/utils": "^0.0.1",
    "@tanstack/react-form": "^1.29.0",
    "jwt-decode": "^4.0.0",
    "react-signature-canvas": "^1.1.0-alpha.2"
  }
}
